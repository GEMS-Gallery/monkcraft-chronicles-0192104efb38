import Text "mo:base/Text";

actor {
  public query func getMonkInfo() : async Text {
    return "The monk class was introduced in the Mists of Pandaria expansion. Monks are masters of martial arts, using their chi to power their abilities.";
  };
}
